from setuptools import setup

setup(name='docx',
      version='0.0.1',
      description='parse docx files and convert them to markdown and html',
      url='git@github.com:jaant/Docx.git',
      author='jaan tallinn',
      author_email='jaan@bluemoon.ee',
      license='MIT',
      packages=['docx'],
      zip_safe=True)
